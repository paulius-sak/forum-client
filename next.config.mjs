/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  env: {
    // SERVER_URL: "http://localhost:3003",
    SERVER_URL: "https://forum-node-js.onrender.com",
  },
};

export default nextConfig;
